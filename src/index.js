/**
 * Media Library Enhance — block editor entry point.
 *
 * Each feature module registers its own filters/subscribers when imported.
 * Compiled to build/index.js via @wordpress/scripts.
 */
import './tags-panel';
import './duplicate-notice';
import './replace-everywhere';
