# Test Run 001 — 2026-04-15

## Failures

### 1. Unit tests can't find WordPress test library
- `composer test` fails: `Could not find WordPress test library at /tmp/wordpress-tests-lib`
- Need WP_TESTS_DIR or auto-install script

### 2. wp-env can't fetch WP 7.0-RC2
- `.wp-env.json` references `WordPress/WordPress#7.0-RC2`
- That tag doesn't exist on github.com/WordPress/WordPress
- Latest stable is 6.9.4
- Decision: pin to `6.9.4` so tests can actually run; user can override when 7.0 ships
